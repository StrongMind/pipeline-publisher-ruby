name: Publish Gem

on: 
  workflow_dispatch:
  
jobs:
  PublishGem:
    uses: Strongmind/public-reusable-workflows/.github/workflows/publish-gem.yml@main
    with:
      ruby_version: '2.5.0'
    secrets: inherit
